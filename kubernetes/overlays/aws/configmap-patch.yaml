apiVersion: v1
kind: ConfigMap
metadata:
  name: ahc-app-config
data:
  # TODO: replace ahc.example-aws-placeholder.com with the real AHC_DOMAIN
  # before the first aws sync. localhost must stay in ALLOWED_HOSTS for
  # kubelet probes.
  ALLOWED_HOSTS: "localhost,127.0.0.1,ahc.example-aws-placeholder.com"
  CSRF_TRUSTED_ORIGINS: "https://ahc.example-aws-placeholder.com"
