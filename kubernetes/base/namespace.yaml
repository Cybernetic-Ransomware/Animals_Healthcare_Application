apiVersion: v1
kind: Namespace
metadata:
  name: ahc
  annotations:
    # Deleting the namespace takes every PVC (including StatefulSet data)
    # with it — require explicit confirmation in ArgoCD.
    argocd.argoproj.io/sync-options: Prune=confirm,Delete=confirm
